# Usado automaticamente por `vite build`.
# Contém apenas valores públicos — tudo com prefixo VITE_ vai para o bundle
# e fica visível para qualquer pessoa. Segredos ficam só no backend.
VITE_API_URL=https://pretty-generosity-production-b8c6.up.railway.app
